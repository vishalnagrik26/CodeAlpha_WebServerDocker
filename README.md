
# CodeAlpha Task 4: Web Server using Docker

This repository contains the setup for running a web server inside a Docker container using Nginx.

## Project Files
- `Dockerfile`: Defines the Nginx container environment.
- `index.html`: Web page served by the web server.

## How to Run

1. **Build the Docker image:**
   ```bash
   docker build -t my-web-server .
